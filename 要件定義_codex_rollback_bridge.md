Git監視素材生成アプリ

# 要件定義

## 0. ナレッジ参照
- target_tags: `rust`, `eframe`, `egui`, `windows`, `git`, `gui`
- knowledge_refs: `KB-EGUI-001`
- 適用対象: 6章の画面表示（文字視認性）

## 1. システム概要
- プロジェクト名: Codex Rollback Bridge
- 目的: Gitリポジトリを常時監視し、Codexに渡すロールバック素材（スナップショット）を生成する。
- 対象形態: Rust製Windowsウィンドウアプリ
- 非対象: ロールバック実行は行わない。

## 2. 用語定義
- プロジェクト: `.git` フォルダが存在するフォルダ
- 監視更新: 指定間隔ごとにGit情報を再取得する処理
- HEAD: 現在参照中コミット（full id, message, datetime を扱う）
- dirty状態: `git status --porcelain` の結果から判定する未コミット変更状態
- ターゲットコミット: コミット一覧の行選択で指定する比較対象コミット
- 最小素材: 指定の最小項目のみを含む素材
- 詳細素材: 最小素材に加えて status/diff 系情報を含む素材
- 状態: 画面上の動作モード（`プロジェクト未選択` `選択済み` `更新中` `エラー状態`）

## 3. システム構成
- 実装言語: Rust
- GUI: `eframe (egui)`
- Git操作: `std::process::Command` で `git.exe` を呼び出す
- 設定保存: JSON
- 非同期モデル:
  - UIスレッド: `eframe (egui)` の描画と入力処理を担当
  - Git監視ワーカースレッド: Gitコマンド実行と監視データ取得を担当
  - UI反映: `egui` フレームごとに最新監視結果をポーリングして反映
  - `tokio` は前提としない
- 論理分離:
  - UIロジック
  - Git監視・取得ロジック
  - 素材生成ロジック

## 4. 機能要件詳細
### 4.1 プロジェクト選択機能
- ルートフォルダを選択する。
- スキャン範囲:
  - デフォルト: 直下のみ
  - オプション: 深さ2まで再帰
- 判定条件: `.git` フォルダが存在するフォルダのみプロジェクトとして扱う。
- 表示情報:
  - フォルダ名
  - パス
  - 現在ブランチ
  - 最終コミット日時
- 選択確定後、メイン画面へ反映し、即時に更新監視を開始する。

### 4.2 常時監視機能
- 更新間隔:
  - デフォルト: 3秒
  - 変更可能
- 毎回更新:
  - 現在ブランチ
  - HEAD（full id, message, datetime）
  - dirty状態（`git status --porcelain`）
- HEAD変更判定:
  - 直前更新時の `head_full_id` と今回取得した `head_full_id` を完全一致比較する。
  - 不一致時のみ HEAD変更と判定する。
  - 初回更新時（直前値なし）は HEAD変更として扱う。
- HEAD変更時のみ更新:
  - コミット一覧（最大50件）

### 4.3 コミット一覧表示
- 件数上限: 50件
- 表示列:
  - `datetime`
  - `short_id`
  - `message`
- `full_id` は内部保持する。
- 行選択でターゲットコミットを指定する。

### 4.4 素材生成機能（最小素材）
- 含める項目:
  - `repo_path`
  - `current_branch`
  - `head_full_id`
  - `head_message`
  - `head_datetime`
  - `target_full_id`
  - `dirty`（true/false）
  - `status_porcelain`
  - `recent_commits`（最大50）
- 出力形式:
  - 人間向けテキスト
  - JSON

### 4.5 素材生成機能（詳細素材）
- 最小素材に加えて以下を含める:
  - `git status`（全文）
  - `git diff`（未ステージ）
  - `git diff --staged`
  - `git diff <target>..HEAD`
- 制限:
  - 最大200KB（`204,800` byte）
  - byte判定基準は「7.3 容量制限判定基準」に従う。
  - 超過時は省略を明記し、変更ファイル一覧と件数のみ出力する。

### 4.6 素材生成の前提条件
- 実行可能条件:
  - 状態が `選択済み` であること
  - ターゲットコミットが選択済みであること
- `target` 未選択時の挙動:
  - 素材生成を開始しない。
  - 生成ボタン押下時は「ターゲットコミット未選択」を明確表示する。
- dirty時の扱い:
  - 素材生成は許可する。
  - `dirty` は必ず `true` として出力する。
  - `status_porcelain` を必ず出力する。
  - 詳細素材では未ステージ/ステージ済み差分を通常どおり出力対象とする。

### 4.7 状態遷移仕様
- 状態一覧:
  - `プロジェクト未選択`
  - `選択済み`
  - `更新中`
  - `エラー状態`
- 遷移定義:
  - `プロジェクト未選択` -> `更新中`: プロジェクト選択確定時
  - `更新中` -> `選択済み`: 監視更新成功時
  - `更新中` -> `エラー状態`: 監視更新失敗時
  - `選択済み` -> `更新中`: 更新間隔到達時、または手動更新実行時
  - `エラー状態` -> `更新中`: 次回更新間隔到達時、または手動再試行時
  - `選択済み` -> `プロジェクト未選択`: プロジェクト選択解除時
  - `エラー状態` -> `プロジェクト未選択`: プロジェクト選択解除時
- 画面要件:
  - 現在状態を常時表示する。
  - `更新中` と `エラー状態` は識別できる表示にする。

### 4.8 エラー処理動作
- `git` が存在しない場合は明確に表示する。
- Gitリポジトリでないフォルダは明確に表示する。
- コマンド失敗時はログ表示する。
- 更新エラー時もアプリは停止しない。
- 再試行ポリシー:
  - 同一更新サイクル内での即時再試行は行わない。
  - 次回更新間隔到達時に再試行する。
  - `git` コマンド失敗が3回連続した場合はエラー表示を強調する。
  - 更新成功時に連続失敗回数を `0` にリセットする。

### 4.9 非対象操作
- 実行しないGit操作:
  - `git reset`
  - `git revert`
  - `stash` 操作
  - branch作成
  - commit操作

## 5. 非機能要件
- UIがフリーズしないこと（3章の非同期モデルを前提とする）。
- GitロジックとUIロジックを分離すること。
- 将来CLI化できる構造にすること。

## 6. 画面仕様
- 画面種別:
  - プロジェクト選択画面
  - メイン画面
- 文字視認性:
  - ライトテーマ時の通常表示テキストは可読性を優先し、薄すぎる表示を許容しない。
  - `text_alpha_from_coverage` に `Linear` を使用しない（既定値またはGamma系を使用）。
  - 必要時は `override_text_color` を用いて通常テキストを濃色に統一する。
- プロジェクト選択画面:
  - ルートフォルダ選択
  - スキャン範囲設定（直下のみ / 深さ2再帰）
  - 候補プロジェクト一覧（フォルダ名、パス、現在ブランチ、最終コミット日時）
  - 選択確定操作
- メイン画面:
  - 状態表示（`プロジェクト未選択` `選択済み` `更新中` `エラー状態`）
  - 監視対象プロジェクト表示
  - 更新間隔設定
  - 現在ブランチ表示
  - HEAD情報表示（full id, message, datetime）
  - dirty状態表示
  - コミット一覧表示（最大50件、行選択）
  - 素材生成操作（最小素材 / 詳細素材）
  - 素材生成操作の有効条件表示（target選択有無）
  - エラー・ログ表示

## 7. データ仕様
### 7.1 設定JSON仕様
- 保存先: `%AppData%\\CodexRollbackBridge\\settings.json`
- 文字コード: UTF-8
- ルート: JSONオブジェクト
- 必須キー定義:
  - `schema_version`: 整数、必須、値は `1`
  - `root_folder_path`: 文字列または `null`、必須、初期値 `null`
  - `scan_scope`: 文字列、必須、`direct` または `depth2`、初期値 `direct`
  - `update_interval_sec`: 整数、必須、範囲 `1..3600`、初期値 `3`
  - `selected_repo_path`: 文字列または `null`、必須、初期値 `null`
- 読み込み時ルール:
  - 必須キー欠落または型不一致時は既定値へフォールバックする。
  - フォールバック発生時はログ表示する。
  - 未定義キーは無視する。

### 7.2 監視データ
- 現在ブランチ
- HEAD情報（full id, message, datetime）
- dirty状態
- `status_porcelain`
- コミット一覧（最大50）
- `previous_head_full_id`（HEAD変更判定用の内部保持値）
- `consecutive_update_failures`（更新失敗連続回数）

### 7.3 容量制限判定基準（byte単位）
- 対象: 詳細素材のみ
- 単位定義:
  - `1KB = 1024 byte`
  - 上限は `200KB = 204,800 byte`
- 判定対象:
  - 1回の素材生成で作成される出力単位ごとに判定する。
  - 人間向けテキストとJSONは別々に判定する。
- 判定タイミング:
  - UTF-8エンコード後のバイト数で判定する。
  - 改行文字を含む最終文字列全体を対象とする。
- 超過時挙動:
  - 超過を明記する。
  - `git status` および各 `diff` の全文は省略する。
  - 代替として、変更ファイル一覧と件数のみを出力する。

### 7.4 素材データ
- 最小素材項目: 4.4 に準拠
- 詳細素材追加項目: 4.5 に準拠
- 出力形式: 人間向けテキスト / JSON
- 容量制限: 詳細素材は 7.3 に準拠

## 8. エラー仕様
- 検出対象:
  - `git` 未導入
  - Gitリポジトリ外フォルダ選択
  - `git.exe` 実行失敗
  - 監視更新処理中エラー
  - 設定JSONの形式不正（欠落、型不一致、JSONパース失敗）
- 表示方針:
  - ユーザーに明確表示する。
  - コマンド失敗のログを表示する。
  - 設定JSONフォールバック発生をログ表示する。
  - 更新失敗が3回連続した場合は強調表示する。
- 継続動作:
  - 監視更新でエラーが発生してもアプリ全体は停止しない。
  - 設定JSONエラー時も既定値で起動継続する。
  - 連続失敗回数は成功時にリセットする。

## 9. 制約事項
- 技術制約:
  - Rust
  - `eframe (egui)`
  - `std::process::Command` による `git.exe` 呼び出し
  - 設定保存はJSON
- 機能制約:
  - ロールバック実行は行わない。
  - 4.9 のGit操作は実行しない。

## 10. 将来拡張ポイント
- CLI化に対応しやすい構造（UIロジックとGitロジック分離）を維持する。
- 設定JSONの `schema_version` を用いた将来バージョン拡張に対応できる構造を維持する。

# 詳細設計
- 未記載（本ファイルは要件定義段階）。

# 手動テスト結果
- 未実施。

# 変更履歴
- 2026-02-18: 初版作成（要件定義を新規作成）。
- 2026-02-18: 状態遷移仕様、素材生成前提条件、設定JSON仕様、容量制限判定基準を追加。
- 2026-02-18: 非同期モデル前提、git失敗時再試行ポリシー、HEAD変更検出方式を追加。
- 2026-02-19: 共通ナレッジ参照（target_tags/knowledge_refs）と文字視認性要件を追加。
