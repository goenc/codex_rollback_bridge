Git監視コミット支援アプリ

# 要件定義

## 0. ナレッジ参照
- target_tags: `rust`, `eframe`, `egui`, `windows`, `git`, `gui`
- knowledge_refs: `KB-EGUI-001`
- 適用対象: 6章の画面表示（文字視認性）

## 1. システム概要
- プロジェクト名: Codex Rollback Bridge
- 目的: Gitリポジトリを常時監視し、Codexに渡すロールバック命令文の生成を支援する。
- 対象形態: Rust製Windowsウィンドウアプリ
- 非対象: ロールバック実行は行わない。

## 2. 用語定義
- プロジェクト: `.git` フォルダが存在するフォルダ
- 監視更新: 指定間隔ごとにGit情報を再取得する処理
- HEAD: 現在参照中コミット（full id, message, datetime を扱う）
- dirty状態: `git status --porcelain` の結果から判定する未コミット変更状態
- 選択行: コミット一覧でユーザーがクリックした行（表示ハイライト用）
- 最小素材: 指定の最小項目のみを含む素材
- 詳細素材: 最小素材に加えて status/diff 系情報を含む素材
- 状態: 画面上の動作モード（`プロジェクト未選択` `選択済み` `更新中` `エラー状態`）

## 3. システム構成
- 実装言語: Rust
- GUI: `eframe (egui)`
- Git操作: `std::process::Command` で `git.exe` を呼び出す
- 設定保存: JSON
- 非同期モデル:
  - UIスレッド: `eframe (egui)` の描画と入力処理を担当
  - Git監視ワーカースレッド: Gitコマンド実行と監視データ取得を担当
  - UI反映: `egui` フレームごとに最新監視結果をポーリングして反映
  - `tokio` は前提としない
- 論理分離:
  - UIロジック
  - Git監視・取得ロジック

## 4. 機能要件詳細
### 4.1 プロジェクト選択機能
- ルートフォルダを選択する。
- メイン画面の「プロジェクト変更」操作でサブダイアログを開き、プロジェクト選択を行う。
- ルートフォルダ選択はフォルダ選択ダイアログによる指定を許可する。
- スキャン範囲は常に直下のみ固定とする。
- 判定条件: `.git` フォルダが存在するフォルダのみプロジェクトとして扱う。
- 表示情報:
  - 要件定義書1行目（`要件定義_*.md`）
  - パス
  - 最終コミット日時
  - 要件定義書が見つからない場合は `（要件定義書なし）` を表示する
- ダイアログ表示時に自動でスキャンを実行する。
- フォルダ選択完了時に自動でスキャンを実行する。
- 選択確定後、`selected_repo_path` を更新してメイン画面へ戻り、即時に更新監視を開始する。

### 4.2 常時監視機能
- 更新間隔:
  - デフォルト: 3秒
  - 変更可能
- 毎回更新:
  - 現在ブランチ
  - HEAD（full id, message, datetime）
  - dirty状態（`git status --porcelain`）
- dirtyの表示文言:
  - `dirty=true` は `変更中`
  - `dirty=false` は `変更なし`
- HEAD変更判定:
  - 直前更新時の `head_full_id` と今回取得した `head_full_id` を完全一致比較する。
  - 不一致時のみ HEAD変更と判定する。
  - 初回更新時（直前値なし）は HEAD変更として扱う。
- HEAD変更時のみ更新:
  - コミット一覧（内部保持は最大50件、表示枠は10件固定）
- 日時表示形式:
  - `YYYY/MM/DD HH:MM`

### 4.3 コミット一覧表示
- 表示枠: 10件固定（コミット不足分は空行表示）
- 内部保持件数上限: 50件
- 表示列:
  - `datetime`
  - `short_id`
  - `message`
- テーブルは各項目境界・各レコード境界を罫線で表示する。
- 行選択:
  - 行内の任意セルクリックで同じ行を選択できる。
  - HEAD行は背景色を薄い緑で表示する。
  - 選択行は背景色を薄いグレーで表示する（HEAD行より優先）。
- 表示優先順位:
  - `選択行` > `HEAD行` > `通常行`
- `full_id` は内部保持する（選択行の追跡用途）。

### 4.4 素材生成機能（廃止）
- 本バージョンでは、最小素材/詳細素材の生成機能および関連UIを提供しない。
- 既存の運用は「コミット一覧」と「ロールバック」機能に集約する。

### 4.7 状態遷移仕様
- 状態一覧:
  - `プロジェクト未選択`
  - `選択済み`
  - `更新中`
  - `エラー状態`
- 遷移定義:
  - `プロジェクト未選択` -> `更新中`: プロジェクト選択確定時
  - `更新中` -> `選択済み`: 監視更新成功時
  - `更新中` -> `エラー状態`: 監視更新失敗時
  - `選択済み` -> `更新中`: 更新間隔到達時、または手動更新実行時
  - `エラー状態` -> `更新中`: 次回更新間隔到達時、または手動再試行時
  - `選択済み` -> `プロジェクト未選択`: プロジェクト選択解除時
  - `エラー状態` -> `プロジェクト未選択`: プロジェクト選択解除時
- 画面要件:
  - 現在状態を常時表示する。
  - `更新中` と `エラー状態` は識別できる表示にする。

### 4.8 エラー処理動作
- `git` が存在しない場合は明確に表示する。
- Gitリポジトリでないフォルダは明確に表示する。
- コマンド失敗時はログ表示する。
- 更新エラー時もアプリは停止しない。
- 再試行ポリシー:
  - 同一更新サイクル内での即時再試行は行わない。
  - 次回更新間隔到達時に再試行する。
  - `git` コマンド失敗が3回連続した場合はエラー表示を強調する。
  - 更新成功時に連続失敗回数を `0` にリセットする。

### 4.9 非対象操作
- 実行しないGit操作:
  - `git reset`
  - `git revert`
  - `stash` 操作
  - branch作成
  - commit操作

### 4.10 ロールバック命令生成機能
- メイン画面に次の2ボタンを配置する。
  - `編集ロールバック`
  - `1コミット戻す`
- 監視データ取得済みのときのみ、いずれかのボタンを有効とする。
- `dirty=true` の場合:
  - `編集ロールバック` のみ有効
  - コピー内容は次の固定コマンドとする。
    - `git -C "{repo_path}" reset --hard HEAD`
    - `git -C "{repo_path}" clean -fd`
- `dirty=false` の場合:
  - `1コミット戻す` のみ有効
  - コピー内容は次の固定コマンドとする。
    - `git -C "{repo_path}" rev-parse --verify HEAD~1`
    - `git -C "{repo_path}" reset --hard HEAD~1`
    - `git -C "{repo_path}" status --short`
    - `git -C "{repo_path}" log --oneline -n 3`
- コピー成功/失敗を画面上で明確表示する。
- 生成内容の先頭に、`repo_path` / `current_branch` / `head_full_id` / `head_message` / `head_datetime` / `dirty` / `status_porcelain` をコメントで付与する。
- アプリ本体はコマンドを実行せず、クリップボードコピーのみ行う。

## 5. 非機能要件
- UIがフリーズしないこと（3章の非同期モデルを前提とする）。
- GitロジックとUIロジックを分離すること。
- 将来CLI化できる構造にすること。

## 6. 画面仕様
- 画面種別:
  - プロジェクト選択画面
  - メイン画面
- 文字視認性:
  - ライトテーマ時の通常表示テキストは可読性を優先し、薄すぎる表示を許容しない。
  - `text_alpha_from_coverage` に `Linear` を使用しない（既定値またはGamma系を使用）。
  - 必要時は `override_text_color` を用いて通常テキストを濃色に統一する。
- プロジェクト選択画面:
  - 「プロジェクト変更」操作で開くサブダイアログとして表示する
  - ダイアログサイズは親ウィンドウの約90%
  - ダイアログは移動不可・リサイズ不可
  - ルートフォルダ選択（フォルダ選択ダイアログ対応）
  - スキャン範囲は直下のみ固定
  - ダイアログ表示時に自動スキャン実行
  - フォルダ選択完了時に自動スキャン実行
  - 候補プロジェクト一覧（要件定義書1行目、パス、最終コミット日時）
  - 候補一覧は罫線付きテーブル表示
  - 選択確定操作
- メイン画面:
  - 親ウィンドウはリサイズ不可
  - ヘッダ領域:
    - 左端に「プロジェクト変更」操作を配置する
    - 1行目（プロジェクト変更行）の上下に余白を設ける
    - 現在プロジェクト表示（`selected_repo_path`）
    - 更新間隔設定
    - 手動更新操作
    - 状態表示（`プロジェクト未選択` `選択済み` `更新中` `エラー状態`）
    - 作業ツリー変更表示（`変更中` / `変更なし`）
    - 2行目の下に余白付きの区切り線を表示する
  - サマリ表示:
    - コミット一覧上部の詳細サマリ（現在ブランチ/HEAD情報/HEAD変更検出）は表示しない。
  - コミット一覧テーブル（中央）:
    - 表示件数: 10件固定（不足分は空行）
    - 列: `datetime` `short_id` `message`
    - 各項目境界・各レコード境界に罫線を表示
    - 行内の任意セルクリックで選択可能
    - HEAD行は薄い緑背景で表示
    - 選択行は薄いグレー背景で表示（HEAD行より優先）
  - 「ロールバック」操作:
    - `編集ロールバック` と `1コミット戻す` の2ボタンを表示
    - `dirty=true` で `編集ロールバック` 有効、`dirty=false` で `1コミット戻す` 有効
    - 固定コマンドのクリップボードコピー
    - 成功/失敗メッセージ表示
  - 状態表示（`プロジェクト未選択` `選択済み` `更新中` `エラー状態`）
  - 監視対象プロジェクト表示
  - 更新間隔設定
  - コミット一覧表示（10件固定、行選択）
  - エラー表示

## 6.1 再現性固定仕様
- ウィンドウ定数:
  - 親ウィンドウ初期サイズ: `1000 x 560`（内寸、リサイズ不可）
  - プロジェクト変更ダイアログサイズ: 親コンテンツサイズの `90%`
  - プロジェクト変更ダイアログ最小値: `640 x 420`
  - プロジェクト変更ダイアログ: 中央配置、移動不可、リサイズ不可
- ヘッダ余白定数:
  - 1行目（プロジェクト変更行）前: `2px`
  - 1行目と2行目の間: `4px`
  - 2行目の下線前: `2px`
  - 下線後: `0px`
- テーブル定数:
  - コミット一覧は左右余白を対称にする（左右マージン同値）
  - コミット一覧表示枠: `10行` 固定（不足分は空セル）
  - ヘッダ行高: `26px`
  - データ行高: `24px`
  - ヘッダ背景: `rgb(236,236,236)`
  - セル枠線: `rgb(144,144,144)`
  - 選択行背景: `rgb(236,236,236)`
  - HEAD行背景: `rgb(228,245,228)`
  - 通常行背景: `rgb(252,252,252)`
- 表示優先順位:
  - 行背景は `選択行` > `HEAD行` > `通常行` を固定する
- イベント順序（UIフレーム）:
  - `monitorイベント取り込み` -> `TopPanel描画` -> `プロジェクト変更ダイアログ初回表示時スキャン` -> `CentralPanel描画` -> `ダイアログ描画` -> `次フレーム要求`
- プロジェクト変更フロー:
  - 「プロジェクト変更」押下でダイアログを開く
  - ダイアログを開いた最初のフレームで自動スキャンを実行する
  - 「フォルダ選択」でフォルダ確定後、即時に自動スキャンを実行する

## 7. データ仕様
### 7.1 設定JSON仕様
- 保存先: `%AppData%\\CodexRollbackBridge\\settings.override.json`
- 文字コード: UTF-8
- ルート: JSONオブジェクト
- 必須キー定義:
  - `schema_version`: 整数、必須、値は `1`
  - `root_folder_path`: 文字列または `null`、必須、初期値 `null`
  - `scan_scope`: 文字列、必須、`direct` または `depth2`、初期値 `direct`
  - `update_interval_sec`: 整数、必須、範囲 `1..3600`、初期値 `3`
  - `selected_repo_path`: 文字列または `null`、必須、初期値 `null`
- 読み込み時ルール:
  - 現行UIでは `scan_scope` は `direct` 固定として扱い、`depth2` が保存されていても `direct` に正規化する。
  - `update_interval_sec` は読み込み後に `1..3600` へ正規化する（範囲外は既定値 `3`）。
  - `selected_repo_path` は実在パスのみ有効とし、存在しない場合は `null` 扱いにする。
  - 保存時は `scan_scope=direct` を常に書き戻す。
  - `override` と `default` の競合時は `override` を優先する。
  - 必須キー欠落または型不一致時は既定値へフォールバックする。
  - フォールバック発生時はログ表示する。
  - 未定義キーは無視する。

### 7.2 監視データ
- 現在ブランチ
- HEAD情報（full id, message, datetime）
- dirty状態
- `status_porcelain`
- コミット一覧（内部最大50、表示10枠固定）
- `previous_head_full_id`（HEAD変更判定用の内部保持値）
- `consecutive_update_failures`（更新失敗連続回数）

### 7.3 容量制限判定基準（byte単位）
- 本バージョンでは素材生成機能を提供しないため、現行UIでは非適用（将来拡張用の仕様保持）。
- 対象: 詳細素材のみ
- 単位定義:
  - `1KB = 1024 byte`
  - 上限は `200KB = 204,800 byte`
- 判定対象:
  - 1回の素材生成で作成される出力単位ごとに判定する。
  - 人間向けテキストとJSONは別々に判定する。
- 判定タイミング:
  - UTF-8エンコード後のバイト数で判定する。
  - 改行文字を含む最終文字列全体を対象とする。
- 超過時挙動:
  - 超過を明記する。
  - `git status` および各 `diff` の全文は省略する。
  - 代替として、変更ファイル一覧と件数のみを出力する。

### 7.4 素材データ
- 本バージョンでは素材生成機能を提供しないため、現行UIでは非適用（将来拡張用の仕様保持）。
- 旧仕様の保持目的でセクションを残し、実装対象からは除外する。

## 8. エラー仕様
- 検出対象:
  - `git` 未導入
  - Gitリポジトリ外フォルダ選択
  - `git.exe` 実行失敗
  - 監視更新処理中エラー
  - 設定JSONの形式不正（欠落、型不一致、JSONパース失敗）
- 表示方針:
  - ユーザーに明確表示する。
  - コマンド失敗のログを表示する。
  - 設定JSONフォールバック発生をログ表示する。
  - 更新失敗が3回連続した場合は強調表示する。
- 継続動作:
  - 監視更新でエラーが発生してもアプリ全体は停止しない。
  - 設定JSONエラー時も既定値で起動継続する。
  - 連続失敗回数は成功時にリセットする。

## 9. 制約事項
- 技術制約:
  - Rust
  - `eframe (egui)`
  - `std::process::Command` による `git.exe` 呼び出し
  - 設定保存はJSON
- 機能制約:
  - ロールバック実行は行わない。
  - 4.9 のGit操作は実行しない。

## 10. 将来拡張ポイント
- CLI化に対応しやすい構造（UIロジックとGitロジック分離）を維持する。
- 設定JSONの `schema_version` を用いた将来バージョン拡張に対応できる構造を維持する。

# 詳細設計
- メイン画面は、監視スナップショット取得後にコミット一覧テーブルを直接表示する構成とした。
- コミット一覧上部の詳細サマリ（現在ブランチ/HEAD情報/HEAD変更検出）はUI表示から削除し、監視・判定は内部ロジックで継続する。
- `head_full_id` の差分判定は `git::fetch_snapshot` 内で継続し、HEAD変更時のみコミット一覧を更新する。
- ヘッダの作業ツリー表示は `dirty` の真偽を `変更中` / `変更なし` に変換して表示する。
- 素材生成機能の廃止に伴い、素材生成UIと `material` モジュールを削除し、Git詳細差分取得の未使用処理を整理した。
- コミット一覧テーブルは3列（日時/短縮ID/メッセージ）に整理し、セル境界と行境界を罫線表示した。
- コミット一覧は表示枠を10行固定とし、コミット不足分は空セル行を描画する方式にした。
- コミット一覧の表示領域は中央寄せし、左右余白が対称になるように調整した。
- 行選択は各セルをクリック可能とし、HEAD行は薄い緑、選択中行は薄いグレー（優先）で表示する。
- ロールバック操作は `編集ロールバック` / `1コミット戻す` の2ボタンに分離し、`dirty` 状態に応じて有効ボタンを切り替える。
- クリップボードにコピーする内容は固定Gitコマンドとし、先頭に `repo/branch/head/dirt/status` のコメントを付与する。
- アプリ本体ではGitコマンドを実行せず、コピーのみを実施する。
- Git日時取得は `--date=format:%Y/%m/%d %H:%M` を使用し、UI表示を同形式へ統一した。
- プロジェクト変更ダイアログは簡略化し、`ワークスペース` ボタン・スキャン範囲切替・手動スキャンボタンを削除した。
- プロジェクト候補スキャンはダイアログ表示時とフォルダ選択時に自動実行し、常に直下探索を適用する。
- プロジェクト候補一覧は罫線付き3列表（要件定義1行目/パス/最終コミット）に変更した。
- 候補取得時に各リポジトリの `要件定義_*.md` の1行目を抽出し、未検出時は `（要件定義書なし）` を表示する。
- プロジェクト変更ダイアログは親サイズの約90%で中央固定し、移動不可・リサイズ不可とした。
- 親ウィンドウはリサイズ不可の固定サイズ（高さ560）で起動するようにした。
- TopBottomPanel の標準境界線は非表示とし、手動区切り線のみ表示する。
- ヘッダ上段/下段の余白を縮小し、コミット一覧が上寄りに見えるように調整した。
- 全ボタンに共通で `2px` の濃色枠線・角丸 `4px`・最小高さ `32px` を適用した。
- 再現性固定仕様として、ウィンドウ定数・余白定数・テーブル定数・表示優先順位・イベント順序を明文化した。
- 設定JSONは `scan_scope=direct` 固定、`update_interval_sec` 正規化、`selected_repo_path` 実在判定を固定運用とした。

# 手動テスト結果
- 未実施。

# 変更履歴
- 2026-02-18: 初版作成（要件定義を新規作成）。
- 2026-02-18: 状態遷移仕様、素材生成前提条件、設定JSON仕様、容量制限判定基準を追加。
- 2026-02-18: 非同期モデル前提、git失敗時再試行ポリシー、HEAD変更検出方式を追加。
- 2026-02-19: 共通ナレッジ参照（target_tags/knowledge_refs）と文字視認性要件を追加。
- 2026-02-19: UI改修反映（プロジェクト変更ダイアログ、コミット一覧状態列、コミット命令コピー機能、素材生成の折りたたみ表示）を追記。
- 2026-02-19: メイン画面ヘッダの配置順（左端にプロジェクト変更）と作業ツリー変更表示文言（変更中/変更なし）を追記。
- 2026-02-19: コミット一覧上部の詳細サマリ表示（現在ブランチ/HEAD情報/HEAD変更検出）を削除する仕様へ更新。
- 2026-02-19: 詳細設計に、サマリ領域削除後のUI表示構成とHEAD差分判定の内部運用を追記。
- 2026-02-19: 素材生成機能および関連UIを廃止し、要件をコミット一覧/コミット命令中心へ更新。
- 2026-02-19: コミット一覧を3列化（罫線付き）し、選択行薄ピンク表示とロールバックHEAD固定仕様へ更新。
- 2026-02-19: 行背景色仕様を更新（HEAD=薄緑、選択=薄グレー優先）。
- 2026-02-19: プロジェクト変更ダイアログを直下固定の自動スキャン方式へ簡略化。
- 2026-02-19: 候補一覧を要件定義1行目中心の3列表へ変更し、ウィンドウ固定サイズ/移動可否/ヘッダ余白を調整。
- 2026-02-19: 再現性固定仕様（UI定数・イベント順序・JSON運用・表示優先順位）を追加。
- 2026-02-19: ロールバック操作を2ボタン化し、dirty連動の固定コマンドコピー方式へ更新。
- 2026-02-19: コミット一覧の表示枠を10行固定に変更し、不足分を空行表示する仕様へ更新。
- 2026-02-19: コミット一覧を中央寄せにして左右余白を対称化し、ヘッダ余白を縮小して一覧を上寄せした。
- 2026-02-19: 親ウィンドウ高さを560へ拡張し、ボタン枠線を2px濃色で統一した。
- 2026-02-19: 設定保存先の表記を実装に合わせて `settings.override.json` へ統一した。
